---
agent: agent
---
Connect to the Sitecore Documentation MCP server.

Connect to the shadcn MCP server.

Create a Sitecore Marketplace app using Next.js. I want the app to appear Full Screen in XMC – Portfolio (Sites). Using XM Cloud APIs including the Sites REST API and GraphQL API, the app should display a Sites like UI allowing to create a site as per the selected site template.

## Project setup
Create the app inside a folder my-sai-sites-app in the my-apps folder.
Make sure the developer server starts on http://localhost:3000.
Install the Marketplace SDK packages at their `latest` versions.